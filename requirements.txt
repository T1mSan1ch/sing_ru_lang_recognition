# Core ML stack — pure PyTorch, Python 3.12 compatible, no OpenMIM
torch==2.6.0
torchvision==0.21.0
transformers>=4.51,<5.0
accelerate>=0.34
open_clip_torch>=2.26
av>=12.0                 # PyAV — fallback video reader
pillow>=10.0
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
pyyaml>=6.0
tqdm>=4.66
matplotlib>=3.8

# Data manifest tooling (already used in dataset adapters)
# (no extra deps beyond pandas)
